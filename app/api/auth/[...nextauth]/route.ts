export { handlers as GET, handlers as POST } from "@/../auth";
// The @/../auth assumes auth.ts is in the root directory.
// If you have a src directory, and auth.ts is in src, it would be:
// export { handlers as GET, handlers as POST } from "@/auth"
